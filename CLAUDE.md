This file mirrors `AGENTS.md` and documents the repository's agent guidance.

Refer to `.github/copilot-instructions.md` for detailed coding conventions.

## Ch10 CRUD Implementation Ready
- Supabase client configured: `lib/supabase/client.ts`
- Auth state ready: `contexts/AuthContext.tsx` with useAuth hook
- Base structure in place: `/posts`, `/posts/new`, `/posts/[id]` pages
- Next step: Connect CRUD operations to Supabase tables
